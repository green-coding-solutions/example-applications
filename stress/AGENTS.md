# Agent Guide

Scope: run the simplest CPU stress benchmark in the repo.

## Scenario

- `usage_scenario.yml`: stresses one core for a short period.

## Entry Files

- `usage_scenario.yml`
- `compose.yml`
- `Dockerfile`

## Agent Notes

- Use this as the baseline stress example when you need a simple, predictable CPU workload.
- `README.md` includes the intended load-shape interpretation and existing badges.
