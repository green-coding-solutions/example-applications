# Agent Guide

Scope: provide a minimal idle baseline measurement.

## Scenario

- `usage_scenario.yml`: sleeps briefly in the `gcb-idle` container.

## Entry Files

- `usage_scenario.yml`
- `compose.yml`

## Agent Notes

- Use this for simple baseline or runner smoke-test work, not for complex behavior.
- The example is intentionally small and should stay easy to reason about.
- See `README.md` for the short build note.
