# Agent Guide

Scope: measure SCI output for a simple idle container.

## Scenario

- `usage_scenario.yml`: idles for a fixed duration and emits SCI-related output.

## Entry Files

- `usage_scenario.yml`
- `compose.yml`

## Agent Notes

- Use this as the SCI baseline within the `green-software-foundation-sci` family.
- The setup is intentionally minimal; most work should happen in the scenario, not the image.
- Read `README.md` for the brief background if needed.
